# NeurIPS 2026 rebuttal draft

Internal drafting note: the 2026 rules allow up to 10,000 characters per review, permit new results, prohibit links and identifying information, and do not permit revised PDFs or new supplementary files during rebuttal. Remove every bracketed [TODO: ...] placeholder before posting.

## Claims that must be kept distinct

- The neural method finds candidates; it does not itself prove global optimality.
- The new odd-cycle theorem is
  \[
  t(C_m,W)\ge p^m-p(1-p)^{m-1}
  \]
  for every graphon \(W\) of edge density \(p\) and every odd \(m\ge3\); for \(m=3\) it reduces to Goodman's inequality. It is sharp at the balanced complete \(k\)-partite graphons, so those are minimisers at \(p=1-1/k\).
- This theorem is a lower bound valid for every graphon. It does **not** prove the stronger exact-optimiser conjecture stated in the appendix at every \(p\), and it does not certify every displayed \(C_7\) candidate.
- The new chordal theorem: for every chordal graph \(H\) (no induced cycle of length greater than 3) whose maximal cliques all have the same size \(r\ge3\), the minimiser of \(\mathbf{(P1)}\) at \(p=1-1/k\), for every integer \(k\ge r\), is the balanced complete \(k\)-partite graphon. This settles 17 cases of the 175-graph study that were previously unproven.
- The new large-deviation theorem is local near the Lubetzky-Zhao phase boundary: for every \(d\)-regular \(H\) with \(d\ge2\), away from the exceptional value \(r=(d-1)/d\), the nonconstant optimiser on the symmetry-breaking side is unique up to relabelling and bipodal in a nontrivial open neighbourhood. It does **not** automatically certify every parameter in Table 6.
- The \(H_6\) construction remains a numerical conjecture; we have a symbolic construction only at \(p=4/5\), without a proof of optimality.
- Lean status, to be stated precisely: the odd-cycle and chordal theorems are formalised in full, with no conditional assumptions. The formalisation of the large-deviation theorem takes some established results from the literature and parts of graphon theory as axioms.

## Optional confidential overview to the AC

We thank the AC and reviewers for identifying four points that were under-documented: cross-run variability, independent validation of candidates, architectural disentanglement, and computational scaling. We agree that the neural optimiser alone is a discovery tool rather than a proof of global optimality, and we will sharpen the paper's language to distinguish (i) recovery of proven optima, (ii) candidates matching independent lower bounds, and (iii) numerically competitive candidates.

**Robustness and numerical validation.** The paper reports best-of-run values without stating the exact protocol. The protocol was: for every instance, one training run for each of eight hyperparameter configurations, namely the two activations (\(\sin\) and \(\mathrm{GeLU}\)) crossed with the four learning rates \(10^{-3}, 3\times10^{-4}, 10^{-4}, 3\times10^{-5}\), reporting the best run. We have now rerun the central claims under a standardized protocol: 16 independent repetitions of the same training on the \(K_3\) benchmark at \(p=7/9\), with the same protocol on the open instances to follow, reporting the objective distribution, held-out population-constraint residual, fitted structural parameters, and success rate rather than only the best run. [TODO: results; open-instance repetitions.] For the triangle upper-tail comparison, the reported values of \(h_q(W)\) and of the constraint are computed by deterministic numerical integration in 64-bit arithmetic, not by Monte Carlo sampling, so the paired differences \(h_q(W_{\mathrm{LZ}})-h_q(W_{\mathrm{ours}})\) contain no sampling error. [TODO: report the differences for all six cells and the constraint residual at the evaluation resolution.]

**Independent mathematical follow-up.** With the help of our framework, we have since proved three theorems.

First, following the learned \(C_5/C_7\) structures, we proved that for every graphon \(W\) of edge density \(p\) and every odd \(m\ge3\),
\[
t(C_m,W)\ge p^m-p(1-p)^{m-1}.
\]
For \(m=3\) this is Goodman's inequality; the theorem extends it to all odd cycles. The bound is nontrivial for \(p>1/2\) and sharp at every balanced complete \(k\)-partite graphon, so those graphons are minimisers at \(p=1-1/k\). This is a lower bound for every graphon, not a classification of the exact minimiser at every density. A journal manuscript is in preparation. The proof is analytic apart from two exact-rational univariate Bernstein positivity certificates for \(m=9\).

Second, we proved that for every chordal graph \(H\) whose maximal cliques all have the same size \(r\ge3\), the minimiser of the density-minimisation problem \(\mathbf{(P1)}\) at \(p=1-1/k\) is the balanced complete \(k\)-partite graphon, for every integer \(k\ge r\). This settles 17 cases of the 175-graph study (page 8) that were previously unproven.

Third, motivated by the bipodal outputs for the large-deviation problem, we proved a local structural theorem. For every \(d\)-regular graph \(H\) with \(d\ge2\) and every nonexceptional point of the Lubetzky-Zhao phase boundary, there is a nontrivial open neighbourhood in which the optimiser on the symmetry-breaking side is unique up to relabelling and bipodal, with parameters and optimal value analytic in \((p,r)\). Consequently, the corresponding conditioned dense random graphs concentrate in cut distance around this bipodal structure. This gives a partial answer to Chatterjee's first open problem, which asks for explicit nonconstant solutions or mathematically provable qualitative properties of them. The result is local and is not being used to claim certification of every Table 6 entry.

These theorems were proved with the help of ChatGPT 5.5 and formalised in Lean 4 with the help of Claude Opus 4.8: the odd-cycle and chordal theorems are formalised in full, while the formalisation of the large-deviation theorem takes some established results from the literature and parts of graphon theory as axioms. Since responses may not include links, we can provide an anonymised link to the formalised proofs through the AC during the discussion phase if the reviewers wish to inspect them. These developments support the intended scientific role of our framework: it produces interpretable structures that can seed rigorous mathematics, while the proofs are separate mathematical work.

**Architecture and cost.** We will add Xia et al. (2023), who use a SIREN to reconstruct graphons from observed graphs with a Gromov-Wasserstein loss, and narrow our novelty claim accordingly. Our setting has no observed target graphon: it directly optimises variational functionals and adds a progressive per-layer encoding, a monotone constraint layer, and symmetry-aware integral estimators. We have also rerun the full ablation suite with the sinusoidal per-layer features replaced by GeLU features, keeping the injection structure fixed, which isolates the sinusoidal component from the per-layer injection; the plain-MLP variant with sinusoidal activations in this suite is a parameter-matched SIREN, serving as the Xia-style backbone control under our objective, solver, and estimator [TODO: results]. Finally, \(2^{28}\) is a one-time post-training evaluation budget, not the training batch size; training uses \(2^{12}\)-\(2^{16}\). The dominant network cost is approximately \(O(Nv(H)^2Ld^2)\), plus \(O(N|\mathcal S|e(H))\) for motif-product aggregation. [TODO: measured runtime/memory table summary.]

## Reviewer 1jpj

Thank you for the careful assessment. We agree that the paper should be more explicit about run variability and about the distinction between an empirical constrained gradient and the population constrained gradient.

**1. Number of runs and robustness.** For every experiment, we ran one training run for each of eight hyperparameter configurations: the two activations (\(\sin\) and \(\mathrm{GeLU}\)) crossed with the four learning rates \(10^{-3}, 3\times10^{-4}, 10^{-4}, 3\times10^{-5}\). The reported value is the best over these eight runs. We have additionally run 16 independent repetitions of the same training under a fixed protocol on \(K_3\) at \(p=7/9\), with the same protocol on the open instances to follow. We will report mean, standard deviation, median/IQR, best value, held-out constraint residual, and the fraction recovering the same structure up to graphon relabelling. [TODO: results; open-instance repetitions.] This directly replaces the ambiguous "best across multiple runs" wording.

**2. Population interpretation of the implicit gradient.** The reviewer is correct that unbiasedness of the density estimator does not imply finite-batch unbiasedness of the complete implicit gradient: the batch also enters the denominator through the root solve, so the implicit gradient is biased at finite batch size, and we will not claim otherwise. It is, however, consistent, by the following argument, which we will add. For a sampled batch \(S\), our formula is the exact gradient of the empirical constrained objective whose root is \(\widehat c_S(\theta)\). By construction of the network, the pre-sigmoid output is bounded, say \(|h_\theta(x,y)|\le M\) for \((x,y)\in[0,1]^2\), so both the empirical root and the population root lie in the compact interval \([\mathrm{logit}(p)-M,\ \mathrm{logit}(p)+M]\). By the uniform law of large numbers, the empirical constraint converges almost surely to the population constraint, uniformly in \(c\) over this interval; by continuity and strict monotonicity in \(c\), the empirical root \(\widehat c_S\) converges almost surely to the population root, and the continuous mapping theorem then gives almost-sure convergence of the implicit gradient to the population constrained gradient as the batch size grows. At fixed checkpoints, we also compared the gradient with a large-batch reference over \(N=2^{10},\ldots,2^{16}\): [TODO: bias/cosine-error result].

**3. Small sigmoid derivatives.** There are two distinct issues. The raw Newton step can indeed become unstable under saturation; Appendix D already describes our clipped Newton update, and we will move this explanation forward. Backpropagation is better behaved than a bare "division by a small number" suggests. Writing \(h_\theta\) for the pre-sigmoid logit, the implicit derivative of the scalar bias has the form
\[
\nabla_\theta \widehat c
=-\sum_i w_i\nabla_\theta h_\theta(x_i),
\qquad w_i\ge0,\quad \sum_iw_i=1.
\]
For a general homomorphism-density constraint, the index \(i\) runs over sampled edge occurrences and the nonnegative weights also include the products of the other edge probabilities. Thus
\[
\|\nabla_\theta\widehat c\|\le \max_i\|\nabla_\theta h_\theta(x_i)\|.
\]
Small sigmoid derivatives shrink numerator and denominator together and do not intrinsically amplify the implicit gradient; the structure is the same as in an attention mechanism, with the normalised weights built from \(\sigma'\) in place of \(\exp\). Clipping is needed for the separate forward root solve. We did not observe numerical instability from this term in any of our experiments.

**4. Why the KL variational problem describes conditioned Erdős-Rényi graphs.** The simplest case already shows where the KL divergence comes from. In \(G(n,q)\), each of the \(N=\binom n2\) edges is independently Bernoulli-\(q\), so the probability that exactly \(aN\) edges are present is
\[
\binom{N}{aN}q^{aN}(1-q)^{(1-a)N}
=\exp\!\Big(-N\big(a\log\tfrac{a}{q}+(1-a)\log\tfrac{1-a}{1-q}\big)+o(N)\Big)
\]
by Stirling's approximation: the exponential cost of an atypical edge fraction \(a\) is the Bernoulli KL divergence between \(a\) and \(q\). The graphon large-deviation principle generalises this from a single edge fraction to a full edge-probability profile \(W\), whose cost is the integrated KL divergence \(h_q(W)\). Conditioning on an atypical \(H\)-density restricts the admissible profiles; the exponential probability of the event is controlled by the minimum of \(h_q\) over them, and the conditioned graphs concentrate near the minimising graphons. We will add this short explanation before the formal variational statement.

Finally, we agree that numerical discovery is not a proof. The intended workflow is that discovery is followed by separate mathematical work, and with the help of our framework we have since proved three theorems. First, for every graphon \(W\) of edge density \(p\) and every odd \(m\ge3\),
\[
t(C_m,W)\ge p^m-p(1-p)^{m-1},
\]
which extends Goodman's inequality (the case \(m=3\)) to all odd cycles and is sharp at the balanced complete \(k\)-partite graphons. Second, for every chordal graph whose maximal cliques all have the same size \(r\ge3\), the minimiser of \(\mathbf{(P1)}\) at \(p=1-1/k\) with integer \(k\ge r\) is the balanced complete \(k\)-partite graphon; this settles 17 previously unproven cases of the 175-graph study. Third, the bipodal large-deviation outputs motivated a theorem proving unique bipodal optimisers in a nontrivial neighbourhood on the symmetry-breaking side of the phase boundary, for every \(d\)-regular pattern graph with \(d\ge2\). These theorems were proved with the help of ChatGPT 5.5 and formalised in Lean 4 with the help of Claude Opus 4.8 (the first two in full; the large-deviation formalisation takes some established results from the literature and parts of graphon theory as axioms); since responses may not include links, we can provide the formalised proofs through the AC during the discussion phase. These are mathematical manuscripts in preparation and demonstrate the intended use of the framework, without retroactively certifying every numerical candidate.

## Reviewer PPij

Thank you. We agree that the sample budget, run count, and relation to Xia et al. should have been explained more clearly.

**Sample size.** The \(2^{28}\) figure is not the training batch size. Training uses \(2^{12}\)-\(2^{16}\) sampled tuples; \(2^{28}\) is a conservative, one-time post-training evaluation budget used only when deterministic high-resolution contraction is infeasible. For any fixed permutation set, the estimator is an average of \(N\) independent tuple contributions in \([0,1]\). Hoeffding's inequality therefore gives
\[
\Pr(|\widehat t-t|\ge\epsilon)\le2e^{-2N\epsilon^2}.
\]
At \(N=2^{28}\), the distribution-free 95% absolute-error bound is approximately \(8.3\times10^{-5}\). This is an upper bound, not a claim that \(2^{28}\) is necessary, and it does not provide a useful relative-error guarantee when the true motif density is extremely small. We will clarify all three points.

**Runs.** For every experiment, we ran one training run for each of eight hyperparameter configurations: the two activations (\(\sin\) and \(\mathrm{GeLU}\)) crossed with the four learning rates \(10^{-3}, 3\times10^{-4}, 10^{-4}, 3\times10^{-5}\), and reported the best run. We have now added 16-repetition statistics on \(K_3\) at \(p=7/9\), with the open instances to follow, reporting the distribution and success rate rather than only the best run: [TODO: summary].

**Relation to Xia, Mishne, and Wang (2023).** Thank you for identifying this important omission. Xia et al. work on graphon learning, a problem in network analysis: given a set of graphs \(\{G_i\}_{i=1}^N\) assumed to be sampled from a common underlying graphon \(W\), estimate \(W\). They introduced implicit neural graphon representations for this task, using a SIREN and a Gromov-Wasserstein reconstruction loss, with extensions to graph generation and graph representation learning. Because the goal there is to reconstruct a graphon close to the true one from finite samples, sharp details of the true graphon are often lost; see, for instance, Figure 3(b) and Figure 9 of Azizpour, Zilberstein, and Segarra (AISTATS 2025). Our task observes no target graphon or graph dataset. We directly optimise homomorphism-density or KL functionals under a density constraint. The contributions specific to this setting are the progressive per-layer input encoding for discontinuous extremisers, the symmetry-aware Monte Carlo estimators, and the embedded monotone constraint solver with implicit differentiation. We will add the citation, narrow our novelty wording so that we do not claim to introduce neural graphon representations, and report a parameter-matched SIREN backbone under our same objective, solver, and estimator — the plain-MLP variant with sinusoidal activations in our ablation suite. [TODO: baseline result.]

**Complexity.** With width \(d\), depth \(L\), batch size \(N\), and cached evaluations on at most \(\binom{v(H)}2\) unordered pairs per tuple, the dominant neural forward/backward cost is approximately
\[
O\!\left(Nv(H)^2Ld^2\right),
\]
with an additional \(O(N|\mathcal S|e(H))\) cost for motif-product aggregation. Activation memory is approximately \(O(Nv(H)^2Ld)\). Monte Carlo absolute error scales as \(N^{-1/2}\), so halving it requires approximately four times as many samples. On one RTX A5000, the full runs reported in the paper ranged from about 30 minutes for \(K_3\) to a few hours for the Petersen graph; our new microbenchmark reports [TODO: seconds/iteration and peak memory].

## Reviewer 6KFF

Thank you. We agree that the paper should explain more clearly what is guaranteed, why the architecture is appropriate, and where it is likely to fail.

**What theory does and does not support.** Our guarantees concern three pieces: the empirical scalar constraint has a unique root; the homomorphism-density estimator is unbiased for a fixed graphon; and the displayed implicit formula is the exact derivative of the empirical constrained problem. Continuous neural graphons can approximate step graphons in \(L^1\), and
\[
|t(H,W)-t(H,U)|\le e(H)\|W-U\|_1,
\]
so representational approximation transfers directly to the motif objective. None of these facts gives global convergence for the nonconvex optimisation, and we will state that limitation more prominently.

Beyond these guarantees, we do not have a theoretical characterisation of when the method succeeds, but we have a consistent empirical observation. The problems we study often have trivial solutions; for instance, the constant graphons \(W\equiv p\) and \(W\equiv q\) are trivial solutions of \(\mathbf{(P1)}\) and \(\mathbf{(P2)}\) respectively. When the trivial solution is nearly as good as the optimum (objective difference below about \(10^{-5}\)), training often converges to the trivial solution. More broadly, the method is best suited to dense-graph problems with one monotone scalar constraint, moderate motif density, and a low-complexity block or geometric optimiser; it is less suitable for sparse limits, extremely rare motifs, large motifs under tight memory, multiple nonmonotone constraints, or landscapes with many competitive local minima.

**Why progressive sinusoidal features.** A step boundary requires increasingly high spatial frequencies for accurate approximation. The early, low-scale features provide coarse block geometry, while later high-scale features provide short paths that refine boundaries without forcing every preceding layer to preserve high-frequency information. The per-layer injection and the increasing frequency schedule play different roles; we agree that the original ablation did not isolate them completely. We have therefore rerun the entire ablation suite of the paper — a plain MLP, the ResNet variant with first-layer-only encoding, our model, the constant encoding scale, the regulariser-based constraint, and the constant learning rate (low and high) — with the sinusoidal features in the per-layer encoding replaced by GeLU features, keeping the injection structure fixed. Comparing each variant with its GeLU counterpart isolates the contribution of the sinusoidal features from that of the per-layer injection; the existing constant-scale control separately tests the multiscale schedule. [TODO: results.] We will also add a boundary-refinement visualisation.

The \(d^{-1/2}\) initialisation of residual matrices controls activation growth with fan-in, while the range of each row of \(U^{(\ell)}\) directly controls the initialised spatial frequency. Increasing \(s(\ell)\) therefore gives later layers access to progressively finer scales without increasing hidden-state magnitude.

**Wavelets.** Yes: the constraint solver and Monte Carlo estimator are representation-agnostic, and localized wavelet or Gabor features are plausible alternatives inside the network. We have, however, tested four exact non-neural representations that parameterise \(W\) directly as a symmetric linear combination of basis functions, with no output nonlinearity: the Fourier, Haar wavelet, Legendre polynomial, and Bernstein polynomial bases, on \(K_3\) minimisation at \(p=7/9\). In direct density optimisation, all four failed to satisfy \(W(x,y)\in[0,1]\): the bound cannot be enforced inside a fixed linear basis, so the iterates leave \([0,1]\) and diverge to meaningless values such as \(t(K_3,W)\approx-2\times10^{6}\). We also fitted each basis to the known optimal solution by least squares, which is the global optimum of supervised training. The Fourier, Legendre, and Bernstein fits still violate the bound on about 45% of the domain, overshooting the step boundaries by about \(\pm0.3\). The Haar wavelet fit does stay in \([0,1]\), but it cannot represent the boundaries of the optimal solution, which do not lie on dyadic points, and yields \(t(K_3,W)=0.4464\), worse than the fixed-grid SBM baseline (\(0.4358\); the optimum is \(98/225\approx0.4356\)). A wavelet parameterisation therefore still needs an output nonlinearity as in our architecture. Within the network, we chose sinusoidal features because they are simple to differentiate and their frequency is controlled directly by \(U^{(\ell)}\); we will discuss wavelet INRs as an important comparison rather than claiming that sinusoids are uniquely suitable.

**Applications.** The intended application is mathematical discovery rather than a downstream node-classification task. The concrete practical outcome is already visible: with the help of our framework, we have since proved three theorems. The learned odd-cycle structures led to the lower bound \(t(C_m,W)\ge p^m-p(1-p)^{m-1}\) for every odd \(m\ge3\), extending Goodman's inequality to all odd cycles; a second theorem determines the minimiser of \(\mathbf{(P1)}\) at \(p=1-1/k\) for every chordal graph whose maximal cliques share a common size \(r\ge3\), settling 17 previously unproven cases of our 175-graph study; and the learned bipodal structures in the large-deviation problem led to a local theorem proving uniqueness and bipodality near the phase boundary for every \(d\)-regular pattern graph with \(d\ge2\). The last theorem describes the typical structure of dense Erdős-Rényi graphs conditioned on an upper-tail event and partially answers Chatterjee's first open problem on nonconstant variational minimisers. These theorems were proved with the help of ChatGPT 5.5 and formalised in Lean 4 with the help of Claude Opus 4.8 (the first two in full; the large-deviation formalisation takes some established results from the literature and parts of graphon theory as axioms). We also note a boundary of applicability: extremal graph theory has practical uses through sparse objects such as expander graphs, but our framework targets the dense regime and does not apply to those sparse problems. Related computational uses include constrained dense-graph generation and rare-event importance-sampling proposals, but we will not claim downstream graph-ML performance that we have not evaluated.

## Reviewer BFdn

Thank you for identifying the central issues. We agree that the method should be positioned as a graphon-variational workflow with problem-specific adaptations, not as the first neural representation of a graphon, and that numerical candidates require independent checks.

**Novelty and Xia et al.** Xia et al. (2023) address graphon learning: reconstructing an unknown graphon from a set of observed graphs assumed to be sampled from it, using a SIREN and a Gromov-Wasserstein loss. Our setting has no observed target graphon: it searches directly over graphons to optimise constrained mathematical functionals. The specific contributions are the progressive per-layer encoding for sharp extremisers, the monotone constraint layer and its implicit derivative, and the symmetry-aware density estimator. We will add Xia et al., narrow our novelty statement, and report a parameter-matched SIREN backbone under the same optimisation loop — the plain-MLP variant with sinusoidal activations in our ablation suite: [TODO: result].

**Sinusoid versus injection.** We agree that the original controls do not fully identify the source of the gain. We have rerun the full ablation suite of the paper (plain MLP, ResNet with first-layer-only encoding, our model, constant encoding scale, regulariser-based constraint, constant learning rate low/high) in two versions each: with the sinusoidal per-layer features, and with GeLU features in place of \(\sin\) while keeping the per-layer injection fixed. Comparing each variant with its GeLU counterpart separates the periodic features from the repeated injection, and the constant-scale variant separately tests the progressive scale. [TODO: main result.]

**Reliability and new proofs.** We agree that a learned graphon is not a certificate. The intended workflow is candidate discovery followed by independent mathematics, and with the help of our framework this has now occurred three times.

First, following the learned \(C_5/C_7\) structures, we proved that for every graphon \(W\) of edge density \(p\) and every odd \(m\ge3\),
\[
t(C_m,W)\ge p^m-p(1-p)^{m-1}.
\]
For \(m=3\) this is Goodman's inequality; the theorem extends it to all odd cycles. The bound is sharp at every balanced complete \(k\)-partite graphon, so those are minimisers at \(p=1-1/k\). It is a lower bound valid for every graphon, not a proof of the exact optimiser away from those sharp densities; in particular, we retain "candidate" language for the displayed \(C_7\) graphons and for \(H_6\). A journal manuscript is in preparation.

Second, we proved that for every chordal graph \(H\) whose maximal cliques all have the same size \(r\ge3\), the minimiser of \(\mathbf{(P1)}\) at \(p=1-1/k\) is the balanced complete \(k\)-partite graphon, for every integer \(k\ge r\). This settles 17 cases of the 175-graph study that were previously unproven, converting learned candidates into proven optima.

Third, motivated by the two-block outputs for \(\mathbf{(P2)}\), we proved the following independent structural result. For every \(d\)-regular \(H\) with \(d\ge2\) and every phase-boundary point with \(r\ne(d-1)/d\), there is a nontrivial open neighbourhood in which, on the symmetry-breaking side, the large-deviation optimiser is unique up to relabelling and bipodal; its block parameters and optimal value are analytic in \((p,r)\). This supplies a mathematically provable qualitative description of nonconstant minimisers in a local regime, partially answering Chatterjee's first open problem. It does not certify every Table 6 parameter, which is why we separately audit those numerical comparisons below.

These theorems were proved with the help of ChatGPT 5.5 and formalised in Lean 4 with the help of Claude Opus 4.8: the odd-cycle and chordal theorems are formalised in full, while the formalisation of the large-deviation theorem takes some established results from the literature and parts of graphon theory as axioms. Since responses may not include links, we can provide the formalised proofs through the AC during the discussion phase if the reviewer wishes to inspect them.

**Lubetzky-Zhao comparison and numerical error.** For this comparison, the values of \(h_q(W)\) and of the constraint are not computed by Monte Carlo sampling: they are evaluated by deterministic numerical integration in 64-bit arithmetic. The reported differences therefore contain no sampling error. [TODO: report the paired differences \(\Delta=h_q(W_{\mathrm{LZ}})-h_q(W_{\mathrm{ours}})\) and the constraint residual at the evaluation resolution for all six cells.] We also note that the suboptimality claim is witnessed by the exhibited graphon itself: any feasible graphon with strictly smaller \(h_q\) shows that \(W_{\mathrm{LZ}}\) is not optimal, independently of run-to-run variability. Cross-run variability instead measures how reliably the method finds such a graphon, and we report it separately: [TODO: cross-run variation]. We will restrict the "outperforms Lubetzky-Zhao" claim to \(K_3\). Regarding \(C_4\) and \(C_5\): the Lubetzky-Zhao construction is not specific to \(K_3\) — it is defined for every \(d\)-regular graph — but its original role is to show that the constant graphon is not optimal, not to propose an optimum. For \(C_4\) and \(C_5\) we therefore use \(W_{\mathrm{LZ}}\) as the available reference construction, without claiming that improving on it implies near-optimality.

An even stronger independent check is possible because the learned profiles are close to bipodal. Under the bipodal assumption, the problem reduces to a 4-dimensional constrained optimisation; we fitted a bipodal graphon, enforced its constraint analytically, and evaluated both \(t(K_3,W)\) and \(h_q(W)\) by their exact finite polynomials: [TODO: fit residual and objective gap]. This certifies that the explicit feasible construction beats the reference without claiming global optimality.

**Run distribution and the 175-graph failures.** For every experiment, the reported value is the best of eight runs, one for each hyperparameter configuration (activations \(\sin\)/\(\mathrm{GeLU}\), learning rates \(10^{-3}, 3\times10^{-4}, 10^{-4}, 3\times10^{-5}\)). The main results now include 16-repetition distributions on \(K_3\) at \(p=7/9\), with the open instances to follow [TODO: results]. For the 11.8% suboptimal sweep cases, we examined the failure modes: [TODO: fraction] converged to a suboptimal local optimum, and [TODO: fraction] failed from training instability, diverging to NaN. We also note that the new chordal theorem above now proves optimality for 17 sweep cases where our flag-algebra bounds were not tight.

**Structured parameterisations.** All the non-neural baselines in Appendix H are expressive enough to represent the optimal solution, so the difference is in optimisation rather than expressiveness. For tree-based and fixed-grid SBM baselines, moving a decision boundary is hard: almost all \(\{0,1\}\) configurations are local optima that cannot be escaped without a reset, so these methods depend strongly on the initialisation and often cannot leave the geometry of the initial solution. Parameterising the boundary directly (second and third figures in Figure 15) also fails, because the optimisation landscape is very sharp and training always converges to the constant solution. We also tested parameterising \(W\) exactly as a linear combination of basis functions (Fourier, Haar wavelet, Legendre, and Bernstein bases, with no output nonlinearity): the bound \(0\le W\le1\) cannot be enforced inside a fixed linear basis, so the density optimisation leaves \([0,1]\) and diverges, and even the least-squares fit to the known optimum violates the bound on about 45% of the domain — except for the Haar basis, which stays bounded but cannot represent the sharp non-dyadic boundaries. The neural network, by contrast, jointly optimises the values of the regions and the boundaries between them, and its optimisation landscape lets the boundary move gradually. A sufficiently well-chosen structured family can often recover a solution after its form is known; the value of the neural search is that it does not require choosing in advance whether the answer is multipartite, diagonal/banded, circular-distance, or bipodal. We have added post-hoc structured fits and [TODO: SBM/SIREN] baselines on \(C_7\), \(H_6\), and triangle \(\mathbf{(P2)}\): [TODO: result]. Agreement with a distilled construction validates interpretability rather than weakening the exploratory role of the network.

**Independent verification for \(\mathbf{(P2)}\).** To our knowledge, no computational lower bound analogous to flag algebras is available for problems like \(\mathbf{(P2)}\). Two empirical checks are possible. One is to sample directly from the conditional distribution \(G\sim G(n,q)\mid t(K_3,G)\ge r^3\) with advanced sampling methods and compare with the trained model, but this is not feasible for large \(n\) and \(r\). The other is the bipodal reduction described above, which we use as our independent check: [TODO: results of the 4-dimensional constrained optimisation].

## Reviewer s6Ge

Thank you. We agree that the best-of-runs presentation obscured the reliability of the discovered structures and that conclusion levels should be labelled more explicitly.

**Initialisation, schedule, and permutation sensitivity.** The reported values are each the best of eight runs, one per hyperparameter configuration (activations \(\sin\)/\(\mathrm{GeLU}\), four learning rates). We additionally ran three sensitivity studies. (i) Initialisation: 16 independent repetitions of the same training on \(K_3\) at \(p=7/9\), with the open instances to follow; we report objective distributions, success rates, and fitted structural parameters after accounting for measure-preserving relabellings [TODO: summary; open-instance repetitions]. (ii) Schedule: at a fixed budget of 20000 epochs, we varied the cosine-annealing learning-rate schedule over 1, 2, 4, 8, and 20 cycles (from \(20000\times1\) to \(1000\times20\)) [TODO: result]. (iii) Monte Carlo permutations: on \(C_5\), we trained with three symmetry sets — the identity only, the two cyclic permutations \((0\,1\,2\,3\,4)\) and \((0\,2\,4\,1\,3)\), and the full permutation set. All these estimators are unbiased; the selected set changes variance and cost, not the target [TODO: result]. For cycles, the main estimator averages all coset representatives, so the choice of representatives does not change the symmetrised estimator.

**Population-level constraint error.** The solver enforces the constraint on its current batch, so empirical residual zero is not itself evidence of population feasibility. We now (i) calibrate \(c\) on an independent large sample, (ii) evaluate the constraint and objective on a disjoint sample or grid, and (iii) report the residual distribution produced by ordinary training-sized solver batches. Across [TODO: instances], the held-out residual was [TODO: max/median/interval]. We will add this quantity to the main result tables.

**Cross-run uncertainty.** We now report mean/SD, median/IQR, best, and structural success rate rather than only a bootstrap interval for the selected run. For the triangle upper tail at \(q=0.10\), the paired improvement over \(W_{\mathrm{LZ}}\) was [TODO] across seeds; the evaluation itself is deterministic numerical integration, so no Monte Carlo interval is involved in the reported differences.

**Analytic constructions and mathematical status.** The \(C_7\) appendix already converts the learned pattern into an explicit low-dimensional construction, but the paper did not clearly separate that candidate formula from a proof. Two clarifications. First, while the conjecture in Section E states a specific construction for \(C_7\) (in fact, for all odd cycles), our trained models do not follow this construction; this does not imply that the trained models are not optimal, since there can be multiple optimal solutions and the stated construction is only one of them. Second, for \(H_6\) we have a symbolic construction only at \(p=4/5\):
\[
W(x,y)=\begin{cases}1&\text{if }|x-y|\in[0.1,0.9],\\0&\text{otherwise},\end{cases}
\]
and we do not have a proof of optimality for it.

Following the learned odd-cycle structures, we have since proved that
\[
t(C_m,W)\ge p^m-p(1-p)^{m-1}
\]
for all graphons and all odd \(m\ge3\), extending Goodman's inequality (the case \(m=3\)) to all odd cycles, sharp at the balanced complete \(k\)-partite graphons. This does not certify the exact \(C_7\) optimiser at every density, and the \(H_6\) structure remains conjectural. We also proved that for every chordal graph whose maximal cliques all have the same size \(r\ge3\), the minimiser of \(\mathbf{(P1)}\) at \(p=1-1/k\) with integer \(k\ge r\) is the balanced complete \(k\)-partite graphon, settling 17 previously unproven cases of the 175-graph study. For \(\mathbf{(P2)}\), the bipodal candidates led to a rigorous local theorem: near every nonexceptional point of the Lubetzky-Zhao phase boundary, on the symmetry-breaking side, the optimiser for every \(d\)-regular \(H\) with \(d\ge2\) is unique up to relabelling and bipodal. These theorems were proved with the help of ChatGPT 5.5 and formalised in Lean 4 with the help of Claude Opus 4.8 (the first two in full; the large-deviation formalisation takes some established results from the literature and parts of graphon theory as axioms); we can provide the formalised proofs through the AC during the discussion phase, since responses may not include links.

On labelling: we will use the following levels consistently. (i) Recovery of a proven optimum, as in Figure 1 (Razborov's theorem), Figure 2 (Reiher's theorem), and Figure 3 (the result of Bennett et al.); (ii) candidate matching an independent lower bound; and (iii) numerically competitive candidate, including candidates improving on a reference construction.

**Baselines and cost.** We extended the fixed-grid/SBM and SIREN baselines to [TODO: C7 instance], [TODO: H6 instance], and [TODO: P2 instance]: [TODO: result]. The dominant per-iteration cost is approximately \(O(Nv(H)^2Ld^2+N|\mathcal S|e(H))\); training uses \(2^{12}\)-\(2^{16}\) samples, while \(2^{28}\) is only one-time final evaluation. Measured seconds/iteration and peak memory are [TODO].

## Experiment priorities

### Priority 0: recover facts before launching jobs

1. Run counts are settled: one run per configuration, eight configurations per experiment (activations \(\sin\)/\(\mathrm{GeLU}\) times learning rates \(10^{-3}, 3\times10^{-4}, 10^{-4}, 3\times10^{-5}\)).
2. Record seeds, learning rates, capacity, stopping epoch, hardware, and whether a final independent calibration of \(c\) was performed.
3. Correct the internal \(C_7,p=2/3\) reference value before any revision: the balanced complete tripartite value is \(14/243\approx0.05761317\), not \(0.05763169\). Avoid volunteering this unrelated typo in rebuttal unless a corrected table is discussed.

### Priority 1: most likely to move scores

1. **High-precision \(\mathbf{(P2)}\) audit.** For all six \(K_3\) upper-tail cells, evaluate each selected graphon and \(W_{\mathrm{LZ}}\) by deterministic numerical integration. Report the paired entropy difference and the constraint residual at the evaluation resolution. Where possible, fit a bipodal graphon and evaluate its constraint and entropy analytically (the 4-dimensional reduction). This directly addresses the smallest \(q=0.10\) gaps.
2. **Standardized seed study.** Use at least 10 seeds for:
   - triangle \(\mathbf{(P2)}\), especially \(q=0.10,r\in\{0.4,0.5,0.6\}\);
   - \(C_7\) at one diagonal/banded density, preferably \(p=5/8\);
   - \(H_6\) at the reported novel-structure density;
   - one known benchmark such as \(K_3,p=7/9\).
   Report mean/SD, median/IQR, best, constraint residual, success rate, and structural-parameter variability.
   *Status:* 16 repetitions on \(K_3,p=7/9\) are running, together with the learning-rate-schedule study (20000 epochs in 1/2/4/8/20 cosine cycles). The open instances (triangle \(\mathbf{(P2)}\) at \(q=0.10\), \(C_7\) at \(p=5/8\), \(H_6\)) are not yet launched — these are the distributions the reviewers weighted most.
3. **Population-gradient diagnostic.** Freeze representative checkpoints; use a very large reference batch; over repeated batches at \(N=2^{10},\ldots,2^{16}\), report gradient bias, relative norm error, and cosine similarity. This is much cheaper than retraining and directly answers Reviewer 1jpj.
4. **175-graph failure breakdown.** For the 11.8% suboptimal cases, count how many are local-optimum convergence versus NaN divergence, to fill the failure-mode fractions promised to Reviewer BFdn.
5. **Runtime microbenchmark.** Report seconds/iteration and peak memory for \(N\in\{2^{12},2^{14},2^{16}\}\), \(d\in\{64,128,256\}\), and representative motifs \(K_3,C_5,C_7,H_6,\) Petersen. Full 20,000-epoch reruns are unnecessary.

### Priority 2: architecture and baselines

1. **Factorized architecture study (running).** The full paper ablation suite (plain MLP, ResNet, our model, constant scale, regulariser, constant LR low/high) is being rerun on \(K_3,p=7/9\) with GeLU features in place of the sinusoidal per-layer features, injection structure fixed. Each variant paired with its GeLU counterpart isolates the sinusoidal component from the per-layer injection.
2. **Xia/SIREN baseline (covered by the ablation).** The plain-MLP variant with sinusoidal activations in the rerun ablation suite is a parameter-matched SIREN, so no separate run is needed; quote its number as the Xia-style backbone control. If feasible, also report the original smaller Xia architecture.
3. **Permutation audit (running).** On \(C_5\), training with three symmetry sets: identity only, the two cyclic permutations \((0\,1\,2\,3\,4)\) and \((0\,2\,4\,1\,3)\), and the full permutation set. If time permits, additionally compare estimator variance and wall time at fixed trained graphons.
4. **Selective baseline extension.** Add fixed-grid/SBM and SIREN results for \(C_7,p=5/8\), the main \(H_6\) case, and \(K_3\) \(\mathbf{(P2)}\) at \(q=0.10,r=0.5\).
5. **Basis-function experiment (done).** The four-basis test (Fourier, Haar wavelet, Legendre, Bernstein; density optimisation and supervised least-squares fit at \(K_3\), \(p=7/9\)) is implemented in `short_exps/other_representations.ipynb`. The numbers quoted to Reviewers 6KFF and BFdn are from the seed-41 run: optimisation diverges in every basis (e.g., Legendre reaches \(t(K_3)\approx-2\times10^6\) before the stopping guard); supervised fits violate \([0,1]\) on 45.7% (Fourier) and 44.8% (Legendre = Bernstein, same polynomial span) of entries; Haar stays in \([0,1]\) with \(t(K_3)=0.446430\).

### Priority 3: useful but lower rebuttal yield

- A wavelet/Gabor INR baseline.
- A full capacity sweep across every motif.
- Downstream graph-classification experiments.
- Broad reruns of all 175 graphs.

These are less likely to change the current decision than resolving the central robustness and \(q=0.10\) precision questions.
