/-
# `eq:constant-A` finite sweep — m = 421  (GENERATED by app_constants_finite_sweep.py; do not hand-edit)

One lemma `constA_m421` covering every admissible case-A pair `(421, r)` (`2 <= r`, `6r < 421`,
i.e. `theta = r/421 <= 1/6`), each discharged by `norm_num` on the exact-rational collapsed value
`gRatA 421 r`.
-/

import OddCycleBound.HighDensity.Sweep.Core

namespace OddCycleBound.HighDensity

set_option exponentiation.threshold 600 in
/-- `eq:constant-A` for every admissible case-A pair `(421, r)`. -/
theorem constA_m421 {r : ℕ} (hr2 : 2 ≤ r) (h6r : 6 * r < 421) : 1 ≤ gRatA 421 r := by
  have hub : r ≤ 70 := by omega
  interval_cases r <;> norm_num [gRatA]

end OddCycleBound.HighDensity
