/-
# `eq:constant-A` finite sweep — m = 247  (GENERATED by app_constants_finite_sweep.py; do not hand-edit)

One lemma `constA_m247` covering every admissible case-A pair `(247, r)` (`2 <= r`, `6r < 247`,
i.e. `theta = r/247 <= 1/6`), each discharged by `norm_num` on the exact-rational collapsed value
`gRatA 247 r`.
-/

import OddCycleBound.HighDensity.Sweep.Core

namespace OddCycleBound.HighDensity

set_option exponentiation.threshold 600 in
/-- `eq:constant-A` for every admissible case-A pair `(247, r)`. -/
theorem constA_m247 {r : ℕ} (hr2 : 2 ≤ r) (h6r : 6 * r < 247) : 1 ≤ gRatA 247 r := by
  have hub : r ≤ 41 := by omega
  interval_cases r <;> norm_num [gRatA]

end OddCycleBound.HighDensity
