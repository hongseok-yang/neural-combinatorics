/-
# `eq:constant-A` finite sweep — m = 251  (GENERATED by app_constants_finite_sweep.py; do not hand-edit)

One lemma `constA_m251` covering every admissible case-A pair `(251, r)` (`2 <= r`, `6r < 251`,
i.e. `theta = r/251 <= 1/6`), each discharged by `norm_num` on the exact-rational collapsed value
`gRatA 251 r`.
-/

import OddCycleBound.HighDensity.Sweep.Core

namespace OddCycleBound.HighDensity

set_option exponentiation.threshold 600 in
/-- `eq:constant-A` for every admissible case-A pair `(251, r)`. -/
theorem constA_m251 {r : ℕ} (hr2 : 2 ≤ r) (h6r : 6 * r < 251) : 1 ≤ gRatA 251 r := by
  have hub : r ≤ 41 := by omega
  interval_cases r <;> norm_num [gRatA]

end OddCycleBound.HighDensity
